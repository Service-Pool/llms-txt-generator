name: Deploy API

on:
  workflow_dispatch:
  # push:
  #   branches: [main]
  #   paths:
  #     - 'apps/api/**'
  #     - 'packages/shared/**'
  #     - '.github/workflows/deploy-api.yml'

jobs:
  deploy:
    uses: Service-Pool/scripts/.github/workflows/reusable-deploy.yml@main
    with:
      working_directory: apps/api
      app_name: llms-txt-api
    secrets: inherit
