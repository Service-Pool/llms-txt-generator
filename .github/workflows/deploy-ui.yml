name: Deploy UI

on:
  workflow_dispatch:
  # push:
  #   branches: [main]
  #   paths:
  #     - 'apps/ui/**'
  #     - 'packages/shared/**'
  #     - '.github/workflows/deploy-ui.yml'

jobs:
  deploy:
    uses: Service-Pool/scripts/.github/workflows/reusable-deploy.yml@main
    with:
      working_directory: apps/ui
      app_name: llms-txt-ui
    secrets: inherit
