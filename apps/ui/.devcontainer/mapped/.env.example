# Docker
COMPOSE_PROJECT_NAME=llms-generator-ui

# Frontend dev-server port (development & production)
PORT=3301
PORT_PROXY=8080

# API Configuration
# Все запросы идут через Caddy (localhost:80)
# Caddy роутит /api/* на API, остальное на UI
PUBLIC_API_URL=""

# Polling Configuration (for generation status)
PUBLIC_POLLING_INTERVAL=2000
PUBLIC_POLLING_MAX_ATTEMPTS=300

# HTTP Configuration
PUBLIC_HTTP_TIMEOUT=180000
